<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moustacheminer.discord_blocks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/discordblocks"
        android:label="Discord Blocks"
        android:roundIcon="@mipmap/discordblocks_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateHidden|adjustPan">
        <activity android:name=".Workspace">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Runtime"></activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
